import { CLIENT_ID } from './twitter-config';

export const TwitterSearch = () => {
// Get Tweet objects by ID, using bearer token authentication
// https://developer.twitter.com/en/docs/twitter-api/tweets/lookup/quick-start

    return (

        <h1>

        </h1>
    );
}